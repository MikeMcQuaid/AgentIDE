#!/bin/bash
# Install Brewfile dependencies and generate AgentIDE.xcodeproj.
set -euo pipefail

cd "$(dirname "$0")/.."

if ! brew bundle check &>/dev/null; then
  brew bundle
fi

xcodegen generate --quiet
